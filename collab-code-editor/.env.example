# URL of the standalone y-websocket sync server (see ../server).
# Points at localhost for local dev; swap for the deployed Railway/Render URL in production.
NEXT_PUBLIC_WS_URL=ws://localhost:8080

# Clerk (accounts). Both come from the API Keys page of a Clerk application at
# dashboard.clerk.com. Signing in is optional for *users* — the guest flow needs
# no account — but these keys are required to deploy. What breaks without them
# is not obvious, so: `next build` still succeeds (proxy.ts doesn't run at build
# time), and `next dev` still boots because @clerk/nextjs falls back to keyless
# mode and provisions a throwaway instance under .clerk/ (gitignored). But
# `clerkMiddleware()` asserts both keys on *every matched request*, and keyless
# mode is development-only, so a production start 500s the entire site.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
