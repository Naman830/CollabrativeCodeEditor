# Top-level safety net. Each workspace (collab-code-editor/, server/) also has
# its own .gitignore — this covers anything created at the repo root.

node_modules/

# env files (can opt-in for committing if needed)
.env*
!.env.example

# misc
.DS_Store
*.pem
*.log
